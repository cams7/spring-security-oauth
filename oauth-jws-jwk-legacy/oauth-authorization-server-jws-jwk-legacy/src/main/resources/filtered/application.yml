server:  servlet:    context-path: /sso-auth-server  port: 8081spring:  security:    user:      name: admin1      password: abc123      roles:      - ADMIN      - USER  
security:  oauth2:    client:      client-id: bael-client      client-secret: bael-secret      access-token-validity-seconds: 180 # 3min      refresh-token-validity-seconds: 300 # 5min      authorized-grant-types:      - client_credentials      - authorization_code      - implicit      - password      - refresh_token      scope:      - READ      - WRITE      registered-redirect-uri:      - http://localhost:8082/spring-security-oauth-resource      resource-ids:      - bael-client      auto-approve-scopes:      - true    authorization:      check-token-access: permitAll()      token-key-access: permitAll()
logging:  level:    org: INFO